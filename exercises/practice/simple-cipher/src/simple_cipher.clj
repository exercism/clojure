(ns simple-cipher)

(defn rand-key
  "Returns a random key"
  []
  ;; function body
  )

(defn encode
  "Encodes text using the specified key"
  [key plaintext]
  ;; function body
  )

(defn decode
  "Decodes text using the specified key"
  [key ciphertext]
  ;; function body
  )
