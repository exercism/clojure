(ns dnd-character)

(defn score-modifier
  "Calculates a score's modifier"
  [score]
  ;; function body
  )

(defn rand-ability
  "Generates a random ability"
  []
  ;; function body
  )

(defn rand-character
  "Generates a random character"
  []
  ;; function body
  )
